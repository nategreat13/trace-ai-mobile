{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "installCommand": "yarn install --frozen-lockfile",
  "buildCommand": "yarn workspace @trace/shared build && yarn workspace @trace/web build",
  "outputDirectory": "projects/web/.next"
}
